[build]
builder = "nixpacks"
buildCommand = "bun install --frozen-lockfile"

[deploy]
startCommand = "bun run backend/src/index.ts"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
